[pytest]

addopts = -v -s
sensitive_url = none

markers =
    smoke: Critical path tests — run first
    regression: Full regression suite
    negative: Invalid input and error-handling scenarios
    schema: Response schema validation tests
    e2e: End-to-end multi-step flow tests
    pet: Pet domain tests
    store: Store domain tests
    user: User domain tests
